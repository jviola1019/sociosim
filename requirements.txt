# SocioSim runtime dependencies (mirrors pyproject [project].dependencies).
# Install with:  python -m pip install -r requirements.txt
# Or install the package + deps editable:  python -m pip install -e .
numpy>=1.26
networkx>=3.2
scipy>=1.11
pyyaml>=6.0
