# Fable Forensic Audit - 20260703_0159
Generated by: claude-fable-5 via subprocess
Runner: scripts/run_fable_audit.ps1
Branch: fix/p0-llm-cache-and-audit-hardening
Commit: d1d71fd

---
Scope note: this is a static forensic audit of the pasted listing only ΓÇö the repo is not on disk in this session, so no tests were executed; line numbers are keyed to the provided file contents.

FINDING A-01 | SEVERITY: P2 | FILE: socio_sim\web\app.py:247
ISSUE: Classifier default operating point (precision 0.90 / recall 0.85) is an unlabeled numeric constant applied to every category; it reads like a measured classifier benchmark but is a scenario assumption with no lineage tag.
EVIDENCE:
```python
prec = _f(body, "classifier_precision", 0.90)
rec = _f(body, "classifier_recall", 0.85)
classifier_targets = {c: {"precision": prec, "recall": rec}
                      for c in CATEGORIES}
```
FIX: `0.90` / `0.85` inline literals -> module-level `DEFAULT_CLASSIFIER_PRECISION = 0.90  # scenario_assumption: illustrative synthetic operating point, not measured` (same for recall) and echo `"provenance": "scenario_assumption"` beside classifier_targets in the result payload.
TEST: assert `result["config"]` (or summary) carries a provenance label for classifier targets; claim_scan-style check that no bare classifier precision/recall default lacks a lineage comment.

FINDING A-02 | SEVERITY: P2 | FILE: socio_sim\web\app.py:286
ISSUE: `eu_optout_rate=0.20`, `human_review_accuracy=0.92`, `appeal_grant_fp_rate=0.70` are unlabeled defaults that strongly resemble real DSA/industry statistics (e.g. "20% EU opt-out") and will be mistaken for external aggregates.
EVIDENCE:
```python
eu_optout_rate=_f(body, "eu_optout_rate", 0.20),
...
human_review_accuracy=_f(body, "human_review_accuracy", 0.92),
...
appeal_grant_fp_rate=_f(body, "appeal_grant_fp_rate", 0.70),
```
FIX: bare defaults -> named constants with lineage comments (`# scenario_assumption` or `# external_aggregate: <source>` if sourced) and surface the label in `/api/meta` `defaults` so the UI can display it.
TEST: assert every default in `_build_config` overrides maps to a constant carrying one of the six lineage tags (lint/claim-scan rule).

FINDING A-03 | SEVERITY: P2 | FILE: socio_sim\web\app.py:352
ISSUE: Campaign economics defaults (`base_ctr 0.012`, `base_cvr 0.05`, `ltv_multiplier 3.0`, `conversion_value 1.0`) mirror well-known display-advertising benchmark figures; unlabeled, they masquerade as measured industry rates.
EVIDENCE:
```python
base_ctr = c_float("base_ctr", 0.012)
base_cvr = c_float("base_cvr", 0.05)
conversion_value = c_float("conversion_value", 1.0)
ltv_multiplier = c_float("ltv_multiplier", 3.0)
```
FIX: inline defaults -> `CAMPAIGN_ECON_DEFAULTS = {...}  # scenario_assumption` and record per-field `"provenance": "user_supplied" | "scenario_assumption_default"` in the normalized spec so `economics_provenance` downstream can distinguish user inputs from defaults.
TEST: `test_campaign_specs_are_normalized_for_persistence` extended: spec omitting base_ctr yields provenance `scenario_assumption_default` for that field.

FINDING A-04 | SEVERITY: P3 | FILE: run.py:88
ISSUE: Implausibility "cutoff 3.0" is printed as a decision threshold with no provenance label (it is the conventional history-matching 3╧â threshold, but nothing says so), so it reads as a project-measured pass/fail bound.
EVIDENCE:
```python
print(f"\nAggregate-fit diagnostics vs legacy benchmark targets "
      f"(implausibility I={a.implausibility:.2f}, cutoff 3.0):")
```
FIX: `cutoff 3.0):` -> `cutoff 3.0 [external_aggregate: Pukelsheim 3-sigma convention, history-matching literature]):` (or a shared `IMPLAUSIBILITY_CUTOFF` constant with the citation comment).
TEST: grep-assert the cutoff appears only via the labeled constant.

FINDING A-05 | SEVERITY: P3 | FILE: socio_sim\web\app.py:265
ISSUE: Profile scale numbers (`quick 1000/168`, `test 200/48`, `standard 10000/672`) are hardcoded in `_build_config` and again in the `/api/meta` `profiles` dict, duplicating whatever `RunConfig.quick/test/standard` define ΓÇö drift between these three sources will silently mis-size SBM blocks and mislabel the UI. `/api/meta` also omits `aggregate_matched_prototype`.
EVIDENCE:
```python
_prof_n = {"quick": 1000, "test": 200, "standard": 10000,
           "aggregate_matched_prototype": 1000}.get(profile, 1000)
```
FIX: hardcoded dicts -> derive from the factories, e.g. `_prof_n = factory().n_agents` (or expose `RunConfig.PROFILE_SCALES`) and build `/api/meta` profiles from the same source.
TEST: assert `app._prof_n`-derived values equal `RunConfig.quick().n_agents` etc. for all four profiles.

FINDING B-01 | SEVERITY: P3 | FILE: run.py:205
ISSUE: CLI decision-facing text asserts the benchmark samples are "licensed" and prints F1/ROC-AUC with no evidence-record linkage ΓÇö an unverified licensing/quality claim on the exact surface the claim scanner is supposed to police.
EVIDENCE:
```python
print("Running classifier component benchmark diagnostics on bundled "
      "licensed benchmark samples...")
...
print(f"  {r['name']:16s} ({r['task']}): F1={r['f1']:.3f} "
```
FIX: "bundled licensed benchmark samples" -> "bundled benchmark samples (license/source: see SOURCE_LEDGER.md <ids>)" and have `evidence_gate.py` verify those ledger ids exist.
TEST: evidence_gate assertion that every benchmark named by `evaluate_all` has a ledger entry with license + source hash.

FINDING B-02 | SEVERITY: P3 | FILE: scripts\claim_scan.py:83
ISSUE: RISKY_TERMS omits the model-validity vocabulary this audit area targets ΓÇö "accuracy", "achieves", "outperforms", "F1", "ROC-AUC", "benchmark(ed)" ΓÇö so an unhedged "achieves 92% accuracy" in docs/UI would pass the scanner.
EVIDENCE:
```python
RISKY_TERMS = [
    r"\bvalidat(?:ed|es|ion|ions)\b",
    r"\bcalibrat(?:ed|es|ion|ions)\b",
```
FIX: add `r"\bachiev(?:es|ed)\b"`, `r"\boutperform(?:s|ed)?\b"`, `r"\baccura(?:cy|te)\b"` (with exemptions for negated/hedged forms via the existing NEGATION_RE path).
TEST: test_claim_scan case: a doc line "the classifier achieves 92% accuracy" is flagged; "does not claim accuracy" is not.

FINDING C-01 | SEVERITY: P2 | FILE: run.py:96
ISSUE: Monte Carlo output is labeled "95% [lo, hi]" regardless of replicate count; with `--replicates 2` (the value the web tests use) a 2-point percentile range is printed as a 95% interval ΓÇö an unsupported coverage claim.
EVIDENCE:
```python
for name, d in a.mc.items():
    lo, hi = d["ci"]
    print(f"  {name:24s} median {d['median']:8.4f}  "
          f"95% [{lo:.4f}, {hi:.4f}]")
```
FIX: `f"95% [{lo:.4f}, {hi:.4f}]"` -> `f"percentile range over N={n_replicates} replicates [{lo:.4f}, {hi:.4f}]"` when `n_replicates < 20`, keeping "95%" only above that threshold (and mirror in the web `mc` payload).
TEST: run_sim with n_replicates=2, capture stdout, assert "95%" absent and "N=2" present.

FINDING C-02 | SEVERITY: P2 | FILE: run.py:88
ISSUE: The CLI unconditionally prints an observed-vs-target comparison table against the legacy benchmark targets, while the web layer deliberately gates this behind `_targets_metadata_complete` (currently always False because every bundled target's evidence is 'unsupported'). CLI users see target-distance comparisons the project itself classifies as unsupportable.
EVIDENCE:
```python
for name, value in a.observed.items():
    spec = a.targets.get(name)
    if spec and value == value:  # skip NaN
        print(f"  {name:22s} observed {value:8.4f}  target "
```
FIX: unconditional table -> apply the same gate: move `_targets_metadata_complete` into a shared module and print `"target comparison suppressed: target evidence is 'unsupported'"` when it returns False.
TEST: run_sim with default bundled targets, assert stdout contains the suppression notice and no "target ... +/-" lines.

FINDING C-03 | SEVERITY: P3 | FILE: scripts\evidence_gate.py:41
ISSUE: The stale-positive-claim check on generated reports covers exactly two literal phrases; a regenerated VALIDATION_REPORT.md saying "fully calibrated" or "validated against production data" passes the gate.
EVIDENCE:
```python
if "highest rung" in text or "validates aggregate" in text:
    errors.append(f"{report}: stale positive claim remains")
```
FIX: two hardcoded substrings -> reuse `claim_scan.RISKY_TERMS` + NEGATION_RE over the three generated reports (import from scripts/claim_scan.py) instead of a private 2-phrase list.
TEST: write a temp BENCHMARK_REPORT.md containing "fully validated"; evidence_gate main() returns 1.

FINDING D-01 | SEVERITY: P2 | FILE: socio_sim\web\app.py:300
ISSUE: `holdout_fraction` is accepted from the web body with no visible lower bound in this layer; if 0.0 passes `cfg.validate()` (only the >1 case is test-covered), ad `lift_*`, `lift_pvalue_raw`, and `lift_significant_bh_fdr` fields are still emitted ΓÇö significance/lift language with no control group, i.e. causal marketing output without the experimental design that justifies it.
EVIDENCE:
```python
holdout_fraction=_f(body, "holdout_fraction", 0.10),
```
and (test contract): `for key in ("... "lift_qvalue_bh", "lift_pvalue_raw", "lift_significant_bh_fdr", ...): assert key in ad_metric`
FIX: accept-any-float -> reject or null-out: `if ads_enabled and holdout_fraction <= 0: raise ValueError("holdout_fraction: lift/significance require a non-empty holdout")` (or emit `lift_* = None` with `economics_provenance="no_holdout_no_lift"`).
TEST: POST `{"holdout_fraction": 0}` -> 400, or completed run has all `lift_*` fields null with the no-holdout provenance tag.

FINDING E-01 | SEVERITY: P1 | FILE: socio_sim\content\llm_cache.py:60
ISSUE: `guard_version` is stored only on blocked entries; accepted entries carry no guard version and `resolve()` serves them without any version check. Bumping BLOCKED_GUARD_VERSION after tightening the semantic guard re-screens only the fail-safe direction (blockedΓåÆretry) while text accepted under the older, weaker guard is served verbatim forever ΓÇö the safety-relevant direction is never re-screened.
EVIDENCE:
```python
self._cache[key] = llm_cache.make_entry(text, "accepted", [])
```
```python
# status == "accepted".
return CacheLookup(hit=True, text=text, degradation=None)
```
FIX: `make_entry(text, "accepted", [])` -> `make_entry(text, "accepted", [], guard_version=llm_cache.BLOCKED_GUARD_VERSION)` in both adapters; in `resolve()`, before the accepted return add `if cached.get("guard_version") != BLOCKED_GUARD_VERSION: return CacheLookup(hit=False, text=None, degradation=None)` (and include guard_version in `record_hash`, see E-03).
TEST: mirror `test_blocked_cache_invalidated_by_deliberate_guard_version_bump` for an accepted entry: bump version, assert transport is re-called and the old accepted text is not served.

FINDING E-02 | SEVERITY: P2 | FILE: socio_sim\content\llm_cache.py:27
ISSUE: The module docstring ΓÇö the authoritative statement of this safety-critical trust model ΓÇö says legacy entries "are trusted as accepted text", but the code (and tests) treat both bare strings and status-less dicts as misses that are re-screened. A maintainer "fixing" resolve() to match its own documentation would reintroduce serving never-screened text (the original R-CLAUDE-P0 class of bug).
EVIDENCE:
```python
- Legacy entries (bare strings, or dicts without a `status` field) predate
  this schema and are trusted as accepted text -- they cannot describe a
  blocked result because that status didn't exist yet when they were
  written.
```
vs. `# Legacy bare-string entry: ... treat as a miss so the text is regenerated and freshly re-screened.`
FIX: docstring bullet -> "Legacy entries (bare strings, or dicts without a `status` field) were never screened under the current guard and are treated as cache misses: regenerated and freshly re-screened."
TEST: doc-consistency assertion in tests/test_claim_scan.py or a docstring grep: `"trusted as accepted" not in llm_cache.__doc__`.

FINDING E-03 | SEVERITY: P3 | FILE: socio_sim\content\llm_cache.py:63
ISSUE: `record_hash` binds only text/status/reason_codes; `guard_version` and `semantic_hash` are outside the tamper envelope, and `semantic_hash` is written but never verified on read ΓÇö a dead integrity field that gives false assurance, and guard_version can be silently edited without detection.
EVIDENCE:
```python
entry = {
    "text": text,
    "semantic_hash": semantic_hash(text),
    "status": status,
    "reason_codes": reason_list,
}
```
FIX: `record_hash(text, status, reason_list)` -> include guard_version in the hashed payload (`{"text":..., "status":..., "reason_codes":..., "guard_version": guard_version}`); either verify `semantic_hash` in `resolve()` or delete the field.
TEST: hand-edit `guard_version` on a stored entry without recomputing record_hash; assert resolve() reports `cache_tampered`.

FINDING E-04 | SEVERITY: P3 | FILE: socio_sim\content\llm_adapter.py:143
ISSUE: A transport success whose content is blocked by the semantic guard returns early without resetting `_fail_streak`, contradicting the stated contract ("Reset on any success"): 2 failures + 1 successful-but-blocked call + 1 failure permanently disables the backend despite a live server.
EVIDENCE:
```python
self.on_degradation(
    f"semantic_mismatch:{','.join(reasons)}; template text used")
return item
```
(no `self._fail_streak = 0` on this path; reset happens only on the accepted path)
FIX: in the blocked branch, add `self._fail_streak = 0` before `return item` (transport succeeded; only content was rejected).
TEST: transport that fails twice, then returns PII-blocked text, then fails once more ΓÇö assert `adapter._disabled is False`.

FINDING E-05 | SEVERITY: P3 | FILE: socio_sim\content\llm_adapter.py:205
ISSUE: Known, documented DNS TOCTOU in the SSRF guard: `validate_llm_url` resolves and validates, then `urlopen` performs its own second DNS lookup ΓÇö a fast-flux/rebinding server can pass validation and have urlopen connect to a forbidden address. Acknowledged in the docstring but left open.
EVIDENCE:
```python
validate_llm_url(self.base_url)
with _NO_REDIRECT_OPENER.open(req, timeout=self.timeout) as resp:  # nosec B310
```
FIX: validate-then-reresolve -> have `validate_llm_url` return the validated IP and connect to `http://<pinned_ip>:<port>/...` with an explicit `Host:` header (as the docstring itself prescribes), eliminating the second lookup.
TEST: extend `test_e02_transport_revalidates_dns_and_blocks_rebind` with getaddrinfo returning a good IP on first call and 169.254.169.254 on the second; assert no connection to the rebound address.

FINDING F-01 | SEVERITY: P2 | FILE: socio_sim\web\app.py:313
ISSUE: `out_dir` is keyed by whole-second timestamp; two POST /api/run jobs started within the same second share one directory, so their `events.jsonl` / `manifest.json` audit records clobber or interleave ΓÇö corrupting exactly the artifacts the tool cites as its reproducibility evidence.
EVIDENCE:
```python
out_dir=f"out/web/{int(time.time())}",
```
FIX: `f"out/web/{int(time.time())}"` -> `f"out/web/{int(time.time())}-{uuid.uuid4().hex[:8]}"` (or pass the job_id into `_build_config`).
TEST: build two configs in a tight loop; assert `cfg1.out_dir != cfg2.out_dir`.

FINDING F-02 | SEVERITY: P3 | FILE: socio_sim\web\app.py:89
ISSUE: Host parsing splits on the last ":" before stripping brackets, so a bare IPv6 loopback Host header `[::1]` (default-port form) parses to `":"` and is rejected ΓÇö a functional break of the allow-list for legitimate IPv6 clients, and evidence the parser is fragile.
EVIDENCE:
```python
host = (headers.get("Host") or "").rsplit(":", 1)[0].strip("[]")
```
FIX: rsplit-then-strip -> bracket-aware parse: `h = headers.get("Host") or ""; host = h[1:h.index("]")] if h.startswith("[") else h.rsplit(":", 1)[0]`.
TEST: `_host_allowed({"Host": "[::1]"})` is True and `_host_allowed({"Host": "[::1]:8765"})` is True (given "::1" in LOOPBACK_HOSTS).

FINDING F-03 | SEVERITY: P3 | FILE: socio_sim\web\app.py:672
ISSUE: GET /api/job iterates the job dict while the worker thread inserts keys (`n_ticks`, `result`, `history_warning`, `error`) into the same dict without the lock ΓÇö CPython raises "dictionary changed size during iteration", giving intermittent 500s during polling.
EVIDENCE:
```python
job = _JOBS.get(route.rsplit("/", 1)[-1])
...
self._send_json({k: v for k, v in job.items() if k != "body"})
```
FIX: `job.items()` -> snapshot under the lock: `with _LOCK: snapshot = dict(job)` then build the response from `snapshot` (and have `_run_job` mutate under `_LOCK` or via single-key assignments only).
TEST: stress test polling /api/job in a thread while a fake job inserts keys in a loop; assert no 500 responses.

FINDING F-04 | SEVERITY: P3 | FILE: socio_sim\web\app.py:838
ISSUE: On a non-loopback bind the mandatory SOCIOSIM_ACCESS_TOKEN is transmitted as a plaintext HTTP header (no TLS anywhere in the stack), so the "network authentication" the RuntimeError message implies is sniffable by any on-path observer; the warning printed does not say this.
EVIDENCE:
```python
raise RuntimeError(
    "Non-loopback bind requires SOCIOSIM_ACCESS_TOKEN. The built-in "
    "token is a CSRF guard, not network authentication.")
```
FIX: warning text -> add "the token travels in cleartext HTTP; put a TLS-terminating reverse proxy in front for any non-trusted network" to both the RuntimeError and the bind warning print.
TEST: assert the serve() warning string mentions TLS/cleartext when host is non-loopback.

FINDING G-01 | SEVERITY: P2 | FILE: socio_sim\web\app.py:114
ISSUE: `_asset_registry` strips every registered image down to a bare path ΓÇö no alt text/description field is passed through for any of the 92 feed-cover/ad-creative/editorial PNGs, so the UI cannot render meaningful `alt` attributes (WCAG 2.2 SC 1.1.1); `/api/creative` likewise serves PNGs with no accompanying text alternative.
EVIDENCE:
```python
return {
    "feed_covers": [web_path(r) for r in rows if r.get("role") == "feed_cover"],
    "ad_creatives": [web_path(r) for r in rows if r.get("role") == "ad_creative"],
```
FIX: path-only lists -> `[{"src": web_path(r), "alt": r.get("alt_text", "")} for r in ...]`, adding `alt_text` to registry.json entries (and have evidence_gate flag registry entries missing it).
TEST: assert every entry in `_asset_registry()["feed_covers"]` has a non-empty `alt` key.

FINDING G-02 | SEVERITY: P3 | FILE: socio_sim\web\app.py:36
ISSUE: The entire UI layer (static/index.html, app.js, style.css) is absent from the audit bundle, so keyboard navigation, focus management, ARIA on the charts/feed/audit-log explorer, and color contrast are unverifiable; nothing server-side (e.g., chart data series with no text-summary fallback) indicates an accessible fallback exists. WCAG 2.2 AA conformance cannot be claimed.
EVIDENCE:
```python
STATIC_DIR = Path(__file__).parent / "static"
...
"style-src 'self' 'unsafe-inline'; script-src 'self'; "
```
FIX: no change server-side -> include static/ in the audit scope and add an automated a11y gate (e.g., axe-core via the existing e2e pytest-playwright extra) to CI before any accessibility claim is made.
TEST: playwright + axe scan of `/` with zero serious/critical violations.

FINDING H-01 | SEVERITY: P2 | FILE: socio_sim\web\app.py:114
ISSUE: `web_path` splits on the forward-slash literal `"socio_sim/web/"` before normalizing backslashes; a registry.json generated on Windows with `socio_sim\web\static\...` paths makes `.split(...)[1]` raise IndexError, which 500s `/api/meta` and bricks dashboard bootstrap ΓÇö on the platform this user runs.
EVIDENCE:
```python
def web_path(rec):
    return "/" + rec["file_path"].split("socio_sim/web/", 1)[1].replace("\\", "/")
```
FIX: split-then-replace -> replace-then-split: `p = rec["file_path"].replace("\\", "/"); return "/" + p.split("socio_sim/web/", 1)[1]` (and return None/skip with a logged warning if the marker is absent).
TEST: registry record with `"file_path": "socio_sim\\web\\static\\assets\\v4\\x.png"` -> `web_path` returns `/static/assets/v4/x.png` without raising.

FINDING H-02 | SEVERITY: P1 | FILE: scripts\evidence_gate.py:31
ISSUE: The sha256 integrity check silently skips any registered asset whose file is missing from disk or whose registry entry has an empty `sha256` ΓÇö a deleted or never-hashed asset passes the gate, directly contradicting the module's own contract ("Fail closed when evidence metadata ... missing").
EVIDENCE:
```python
expected_sha = asset.get("sha256", "")
if expected_sha and fp.is_file():
    actual_sha = sha(fp)
    if actual_sha != expected_sha:
```
FIX: skip-on-missing -> fail-closed: `if not expected_sha: errors.append(f"asset {...}: no sha256 in registry"); elif not fp.is_file(): errors.append(f"asset {...}: file missing"); elif sha(fp) != expected_sha: errors.append(...)`.
TEST: delete one registered PNG (or blank its sha256 in a temp registry copy); assert `main()` returns 1 with a "file missing"/"no sha256" error.

FINDING H-03 | SEVERITY: P3 | FILE: socio_sim\web\app.py:37
ISSUE: `_CONTENT_TYPES` has no `.json` mapping, so `/static/assets/v4/registry.json` is served as `application/octet-stream` under `X-Content-Type-Options: nosniff`, which breaks any strict-MIME consumer (e.g., `fetch(...).json()` guarded by content-type checks) of the registry.
EVIDENCE:
```python
_CONTENT_TYPES = {".html": "text/html; charset=utf-8",
                  ".css": "text/css; charset=utf-8",
                  ".js": "text/javascript; charset=utf-8",
```
FIX: add `".json": "application/json; charset=utf-8",` to `_CONTENT_TYPES`.
TEST: GET `/static/assets/v4/registry.json`; assert Content-Type is `application/json; charset=utf-8`.

---
VERDICT: PASS FOR LOCAL RESEARCH USE ONLY
REASON: The safety-critical invariants (blocked cache entries never served, tamper detection, SSRF/host guards, honest classifier naming) hold and are test-backed, but a P1 guard-versioning gap on accepted cache entries, a P1 fail-open evidence gate, unlabeled scenario constants, and an unauditable UI layer bar any stronger rating.
---
TOP 5 PRIORITY FIXES (by risk):
1. E-01 - Accepted cache entries carry no guard_version, so tightening the semantic guard never re-screens previously accepted LLM text ΓÇö stale weak-guard output is served indefinitely.
2. H-02 - evidence_gate.py silently skips missing asset files and empty sha256 fields, so the "fail closed" integrity gate passes with deleted or unhashed assets.
3. F-01 - Whole-second `out_dir` timestamps let concurrent web runs clobber each other's events.jsonl/manifest.json, corrupting the audit trail the tool's reproducibility claims rest on.
4. E-02 - The llm_cache trust-model docstring says legacy entries are "trusted as accepted", contradicting the safe code path; a doc-faithful "fix" would reintroduce the original P0 cache-bypass.
5. C-02 - The CLI prints observed-vs-target benchmark comparisons that the web layer deliberately suppresses because every bundled target's evidence is classified 'unsupported'.
