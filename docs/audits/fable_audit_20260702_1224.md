# Fable Forensic Audit - 20260702_1224
Generated by: claude-fable-5 via subprocess
Runner: scripts/run_fable_audit.ps1
Branch: fix/p0-llm-cache-and-audit-hardening
Commit: 54a4b1e

---
Audit complete. All findings were verified against the working-tree files (line numbers confirmed), plus the static UI assets (index.html, app.js, style.css, registry.json) that areas G and H require but which weren't in the embedded snapshot. The headline result: **no P0**, one **P1** residual bypass in the safety-critical cache trust path (legacy-entry downgrade defeats the blocked-cache guarantee within the module's own stated threat model), and a cluster of P2s around silent degradation, adapter drift, and gate exemptions.

---

FINDING [E1] | SEVERITY: P1 | FILE: socio_sim/content/llm_cache.py:88
ISSUE: Legacy trust paths defeat the blocked-cache invariant under the module's own stated threat model ("naive tampering"). Deleting the `status` + `record_hash` fields from a blocked entry (or replacing the whole entry with a bare string) requires no hash forging, yet converts guard-blocked text into a served "accepted" hit — `resolve()` trusts any dict without `status` (line 119-120) and any non-dict (line 88-90) verbatim, with no re-screening.
EVIDENCE:
```python
if not isinstance(cached, dict):
    # Legacy bare-string entry: predates status/tampering fields.
    return CacheLookup(hit=True, text=str(cached), degradation=None)
...
# status == "accepted", or a legacy dict without a status field.
return CacheLookup(hit=True, text=text, degradation=None)
```
FIX: Legacy entries (bare strings, dicts lacking `record_hash`) → return a `needs_rescreen=True` marker instead of an unconditional hit; adapters run `check_generated_text(lookup.text, item)` before `_apply_text`, degrading to template on failure. Preserves offline replay for clean legacy text; closes the downgrade hole.
TEST: `test_blocked_entry_downgraded_to_legacy_is_not_served` — write a blocked entry, delete `status` and `record_hash`, reload with fresh adapter, assert blocked text absent and a degradation raised; repeat with the entry replaced by a bare string containing PII.

FINDING [E2] | SEVERITY: P2 | FILE: socio_sim/content/llm_cache.py:127
ISSUE: `load()` claims "failing safe (empty cache) on any corruption" but returns any valid-JSON value uninspected: a cache file containing `[]`, `"x"`, or `3` parses fine, then `self._cache.get(key)` raises AttributeError and crashes `generate()` mid-run. `OSError`/`PermissionError` on read are also uncaught.
EVIDENCE:
```python
try:
    return json.loads(path.read_text(encoding="utf-8"))
except (json.JSONDecodeError, UnicodeDecodeError) as exc:
```
FIX: `data = json.loads(...)` then `if not isinstance(data, dict): on_error(...); return {}`; add `OSError` to the except tuple.
TEST: `test_non_dict_json_cache_treated_as_empty` — write `"[]"` to the cache path, assert `generate()` succeeds and a corruption degradation is reported.

FINDING [E3] | SEVERITY: P2 | FILE: socio_sim/content/llm_adapter.py:110
ISSUE: After the circuit breaker trips, every subsequent generation silently serves template text — the code comment literally says "serve template silently", directly contradicting the module docstring guarantee (lines 11-12: "Failures degrade loudly … never silently"). `n_degradations` in the web payload (app.py:590) and the CLI degradation count then under-report actual fallbacks.
EVIDENCE:
```python
if self._disabled:
    return item  # already gave up; serve template silently
```
FIX: `if self._disabled: self.on_degradation(f"{self.backend} disabled; template text used"); return item` — every fallback stays audit-visible.
TEST: Extend `test_engine_runs_with_ollama_mode_offline` — assert the count of degradation events ≥ the number of posts generated after the breaker trips, not just ≥ 1.

FINDING [E4] | SEVERITY: P2 | FILE: socio_sim/content/claude_adapter.py:93
ISSUE: ClaudeAdapter lacks the response normalization LLMAdapter has (llm_adapter.py:113-116): no empty-response rejection, no whitespace collapse, no 280-char cap. An empty Claude response is cached as `accepted` forever and served as blank post text; oversized/multi-line text is cached and replayed verbatim. This is exactly the adapter drift llm_cache.py's docstring says the shared module exists to prevent.
EVIDENCE:
```python
text = resp.content[0].text.strip()
reasons = check_generated_text(text, item)
```
FIX: `text = resp.content[0].text.strip()` → `text = " ".join((resp.content[0].text or "").split())[:280]` + `if not text: raise ValueError("empty LLM response")`, mirroring llm_adapter.py:113-116 (or hoist normalization into llm_cache/a shared helper).
TEST: `test_claude_empty_response_degrades_not_cached` — stub client returning `"  "`, assert template text served, degradation raised, nothing cached.

FINDING [E5] | SEVERITY: P3 | FILE: socio_sim/content/llm_adapter.py:128
ISSUE: `_fail_streak` resets only on an accepted response; a successful transport call whose text is guard-blocked returns at line 125 without resetting. The init comment (line 69) promises "Reset on any success", so a healthy server can be declared unreachable after interleaved failures and blocked-successes, disabling LLM mode for the rest of the run.
EVIDENCE:
```python
self._cache[key] = llm_cache.make_entry(text, "accepted", [])
self._save_cache()
self._fail_streak = 0
```
FIX: Move `self._fail_streak = 0` to immediately after the successful transport call (after line 116), before the guard check.
TEST: `test_blocked_response_resets_fail_streak` — 2 transport failures, 1 blocked-success, 1 failure: assert adapter is not disabled and still calls the transport.

FINDING [E6] | SEVERITY: P3 | FILE: socio_sim/content/llm_cache.py:67
ISSUE: `semantic_hash` is written into every cache entry but never read anywhere in the codebase (grep confirms the only reference outside its definition is this write), and `record_hash` (line 52-58) does not bind `guard_version` — the entry carries a dead integrity field implying coverage it doesn't provide.
EVIDENCE:
```python
entry = {
    "text": text,
    "semantic_hash": semantic_hash(text),
```
FIX: Either verify `semantic_hash` in `resolve()` alongside `record_hash`, or remove the field; add `guard_version` to the `record_hash` payload dict.
TEST: `test_semantic_hash_mismatch_discards_entry` (if kept) or a schema test asserting the field is absent (if removed).

FINDING [E7] | SEVERITY: P3 | FILE: socio_sim/web/app.py:527
ISSUE: The ollama bootstrap checks and starts the hardcoded `_LLM_HOST` (127.0.0.1:11434) while the run itself consumes `cfg.llm_base_url`, which may point at a different local port — the server started is never the one used, and `/api/meta`'s `llm_available` (line 723) reports the wrong endpoint.
EVIDENCE:
```python
if cfg.content_mode == "ollama" and not server_up(_LLM_HOST):
    job["phase"] = "starting local LLM"
    ensure_server(_LLM_HOST, log=...)
```
FIX: Derive the bootstrap host from `cfg.llm_base_url` when set (`urlparse(cfg.llm_base_url).netloc or _LLM_HOST`), falling back to `_LLM_HOST`.
TEST: `test_ollama_bootstrap_targets_config_base_url` — config with `llm_base_url="http://127.0.0.1:11500"`, assert `server_up`/`ensure_server` are called with `127.0.0.1:11500`.

FINDING [F1] | SEVERITY: P3 | FILE: socio_sim/web/app.py:94
ISSUE: The Host allow-list passes requests with an absent/empty Host header (`not host` short-circuit) — an HTTP/1.0 or raw-socket client bypasses the DNS-rebinding read guard entirely.
EVIDENCE:
```python
host = (headers.get("Host") or "").rsplit(":", 1)[0].strip("[]")
...
return not host or host in allowed
```
FIX: `return not host or host in allowed` → `return host in allowed` (all modern browsers and urllib always send Host; the empty-host case only helps an attacker's raw client).
TEST: `test_hostless_request_rejected` — raw socket `GET /api/meta HTTP/1.0\r\n\r\n`, assert 403.

FINDING [F2] | SEVERITY: P3 | FILE: socio_sim/web/app.py:797
ISSUE: `Content-Disposition` interpolates the raw, un-sanitized `run_id` from the URL path — a `run_id` containing `"` breaks out of the quoted filename (header-value injection; no CRLF possible via the request line, but filename spoofing on download is).
EVIDENCE:
```python
self.send_header("Content-Disposition", f'attachment; filename="sociosim-{run_id}-{name}"')
```
FIX: Sanitize before interpolation: `run_id = re.sub(r"[^A-Za-z0-9_-]", "", run_id)` at the top of `_export_run`.
TEST: `test_export_run_id_sanitized_in_disposition` — request `/api/runs/ab%22cd/export`, assert the header contains no `"` beyond the delimiters.

FINDING [F3] | SEVERITY: P3 | FILE: socio_sim/web/app.py:99
ISSUE: `_JOBS` grows without bound — every POST adds an entry (line 844-846) holding the full multi-MB result payload, retained even after persistence to `_STORE` (line 598); a long-lived console session leaks memory monotonically.
EVIDENCE:
```python
_JOBS: dict = {}
...
_JOBS[job_id] = {"status": "running", "progress": 0.0, ...}
```
FIX: After `_STORE.save` succeeds, drop the in-memory payload (`job["result"] = {"persisted": True}` served from `_STORE` on GET), or evict finished jobs beyond the newest N (e.g. 20).
TEST: `test_finished_jobs_evicted` — POST N+1 runs to completion, assert `len(_JOBS)` ≤ N or that old jobs' `result` payloads are released.

FINDING [F4] | SEVERITY: P3 | FILE: pyproject.toml:15
ISSUE: Dependencies are floor-pinned only (`numpy>=1.26`, …) with no lockfile, and `pip-audit` lives in an optional `security` extra with no gate wiring — the CVE posture of any given install is unverifiable and drifts silently.
EVIDENCE:
```toml
dependencies = [
    "numpy>=1.26",
    "networkx>=3.2",
```
FIX: Add a committed constraints/lock file (`requirements.lock` via `pip-compile` or `uv lock`) and run `pip-audit` in CI alongside the claim/evidence gates.
TEST: CI job asserting `pip-audit --strict` exits 0 against the lockfile.

FINDING [A1] | SEVERITY: P2 | FILE: socio_sim/web/app.py:248
ISSUE: Empirically-flavored parameter defaults are hardcoded with no data-lineage label anywhere in the request/response cycle: `classifier_precision 0.90` / `recall 0.85` (248-249), `eu_optout_rate 0.20` (287), `human_review_accuracy 0.92` (308), `appeal_grant_fp_rate 0.70` (310). "EU opt-out rate = 20%" reads like a measured platform statistic; nothing in the exported config marks it scenario_assumption, so downstream readers of `result.json` can mistake defaults for measurements.
EVIDENCE:
```python
prec = _f(body, "classifier_precision", 0.90)
rec = _f(body, "classifier_recall", 0.85)
...
eu_optout_rate=_f(body, "eu_optout_rate", 0.20),
```
FIX: Emit a `parameter_provenance` map in the run payload (all these keys → `"scenario_assumption"` unless user-supplied → `"user_supplied"`), sourced from one dict next to the defaults.
TEST: `test_config_payload_labels_parameter_provenance` — run a job, assert `result["parameter_provenance"]["eu_optout_rate"] == "scenario_assumption"`, and `"user_supplied"` when the body sets it.

FINDING [A2] | SEVERITY: P3 | FILE: socio_sim/web/app.py:281
ISSUE: The SBM connectivity matrix `[[0.02, 0.002], [0.002, 0.02]]` is a hardcoded, unlabeled numeric constant not overridable via the form (unlike every other graph parameter), and carries no scenario_assumption marker.
EVIDENCE:
```python
graph_params = {"block_sizes": [_half, _n - _half],
                "p_matrix": [[0.02, 0.002], [0.002, 0.02]]}
```
FIX: Label in a trailing comment as scenario_assumption and/or expose `graph_p_in`/`graph_p_out` form fields with these as defaults, included in the A1 provenance map.
TEST: Covered by A1's provenance test plus `test_sbm_p_matrix_overridable` if exposed.

FINDING [A3] | SEVERITY: P3 | FILE: run.py:114
ISSUE: The implausibility acceptance threshold "cutoff 3.0" is printed (also line 219) with no provenance label — a user cannot tell whether 3.0 is a measured criterion, a literature convention (e.g. the 3-sigma history-matching cutoff), or arbitrary.
EVIDENCE:
```python
print(f"\nAggregate-fit diagnostics vs legacy benchmark targets "
      f"(implausibility I={a.implausibility:.2f}, cutoff 3.0):")
```
FIX: `cutoff 3.0` → `cutoff 3.0 (history-matching convention, scenario_assumption)` sourced from a single named constant `IMPLAUSIBILITY_CUTOFF = 3.0` with a citation comment.
TEST: Grep-style assertion in `test_claim_scan.py` or a unit test that the CLI output labels the cutoff's provenance.

FINDING [B1] | SEVERITY: P3 | FILE: socio_sim/web/app.py:724
ISSUE: `/api/meta`'s `profiles` dict omits `aggregate_matched_prototype` even though `_PROFILES` (line 164) accepts it as a public choice — the UI has no scale metadata (n_agents/n_ticks) for a selectable profile, presenting inconsistent capability information.
EVIDENCE:
```python
"profiles": {"quick": {"n_agents": 1000, "n_ticks": 168},
             "test": {"n_agents": 200, "n_ticks": 48},
             "standard": {"n_agents": 10000, "n_ticks": 672}},
```
FIX: Add `"aggregate_matched_prototype": {"n_agents": 1000, "n_ticks": 168}` (matching `_prof_n`/`_prof_ticks` at lines 265-268 — ideally derive both surfaces from one dict).
TEST: `test_meta_profiles_cover_all_public_profiles` — assert `set(meta["profiles"]) == app._PROFILES`.

FINDING [B2] | SEVERITY: P3 | FILE: tests/test_web.py:187
ISSUE: `"RUN REPORT" not in result` tests dict KEYS, not report content — vacuously true regardless of what `report_md` contains, so the assertion the comment claims ("report present" / no stale heading) never actually runs. This repo's audit history (PR #4) is precisely about tests that assert less than they appear to.
EVIDENCE:
```python
assert "report_md" in result and "RUN REPORT" not in result  # report present
```
FIX: `"RUN REPORT" not in result` → `"RUN REPORT" not in result["report_md"]` (or the positive containment check actually intended).
TEST: The corrected line itself; verify it can fail by temporarily injecting the phrase into the report renderer.

FINDING [C1] | SEVERITY: P2 | FILE: scripts/claim_scan.py:85
ISSUE: `CALIBRATION_REPORT.md` — by name the single most calibration-claim-prone surface in the repo — is fully exempt from the context-aware risky-term layer, AND `evidence_gate.py:27` scans only BENCHMARK/BACKTEST/VALIDATION reports for stale positive claims. A regression reintroducing "calibrated against real data"-style language into CALIBRATION_REPORT.md passes every gate.
EVIDENCE:
```python
CONTEXT_LAYER_FILE_EXEMPT = {
    "CHANGELOG.md",
    "PLAN_P2.md",
    "CALIBRATION_REPORT.md",
}
```
FIX: Remove `"CALIBRATION_REPORT.md"` from `CONTEXT_LAYER_FILE_EXEMPT` (hedging its genuine retrospective lines with the existing negation vocabulary), or minimally add it to `evidence_gate.py`'s stale-positive-claim report tuple.
TEST: `test_calibration_report_is_a_scanned_claim_surface` — write "the simulator is calibrated to production data" into a temp CALIBRATION_REPORT.md fixture and assert the gate fails.

FINDING [C2] | SEVERITY: P3 | FILE: scripts/claim_scan.py:165
ISSUE: Any negation token anywhere in the 60-char look-back window clears a risky term even when grammatically unrelated — "not slow, and fully validated against production data" passes the scan. The hedge heuristic is biased toward false negatives on its safety-critical purpose.
EVIDENCE:
```python
window = scan_text[start:m.end() + TRAILING]
if NEGATION_RE.search(window):
    continue
```
FIX: Require the negation within the same sentence/clause: split the window at `.;:` and only search the final segment containing the match, or shrink WINDOW to ~30 chars.
TEST: `test_unrelated_negation_does_not_hedge` — assert "not slow, and fully validated against production data" is flagged while "not a validation claim" still passes.

FINDING [D1] | SEVERITY: P3 | FILE: socio_sim/web/app.py:373
ISSUE: Campaign economics defaults — `base_ctr 0.012`, `base_cvr 0.05`, `conversion_value 1.0`, `ltv_multiplier 3.0`, `attribution_window_ticks 168` — feed LTV/lift/ROAS-style outputs but carry no lineage marker in the `campaign_specs` echoed back in the payload (line 565). Outputs carry `economics_provenance` (per test_web.py:196), but the inputs that drive them look like industry benchmarks and are labeled nothing.
EVIDENCE:
```python
base_ctr = c_float("base_ctr", 0.012)
base_cvr = c_float("base_cvr", 0.05)
...
ltv_multiplier = c_float("ltv_multiplier", 3.0)
```
FIX: In `_normalize_campaign_specs`, tag each cleaned spec with `"input_provenance": "user_supplied"` when the key was present in the body, else `"scenario_assumption_default"`.
TEST: `test_campaign_specs_carry_input_provenance` — spec omitting base_ctr → provenance shows the default is a scenario assumption.

FINDING [G1] | SEVERITY: P3 | FILE: socio_sim/web/app.py:112
ISSUE: `_asset_registry()` strips the registry's `accessibility_alt_template` field (present on every record, registry.json:4) — only bare paths reach the client, so app.js:804 compensates with one hardcoded generic alt string. The per-asset accessible-name channel is dead; any future role-specific alt text silently never renders (WCAG 1.1.1 fidelity).
EVIDENCE:
```python
def web_path(rec):
    return "/" + rec["file_path"].split("socio_sim/web/", 1)[1].replace("\\", "/")
return {
    "feed_covers": [web_path(r) for r in rows if r.get("role") == "feed_cover"],
```
FIX: Return `{"path": web_path(r), "alt": r.get("accessibility_alt_template", "").format(role=r.get("role", "asset"))}` per asset and consume `alt` in app.js's `<img>` construction.
TEST: `test_asset_registry_exposes_alt_text` — assert every entry in `meta["assets"]["feed_covers"]` has a non-empty `alt`.

FINDING [G2] | SEVERITY: P3 | FILE: socio_sim/web/static/style.css:127
ISSUE: Form fields replace the focus outline with a box-shadow (`outline: none`). In Windows High Contrast / `forced-colors: active` mode, box-shadows are removed by the UA, leaving keyboard focus invisible on every input and select (WCAG 2.4.7 Focus Visible).
EVIDENCE:
```css
.field input:focus, select:focus { outline: none; border-color: var(--blue); box-shadow: 0 0 0 4px rgba(10,132,255,.14); }
```
FIX: `outline: none` → `outline: 2px solid transparent; outline-offset: 1px` (transparent outlines are repainted in system colors under forced-colors, preserving the shadow look elsewhere).
TEST: Manual/Playwright check with `forced-colors: active` emulation — tab to an input, assert a visible focus indicator (screenshot or computed outline-color ≠ transparent under forced colors).

FINDING [H1] | SEVERITY: P3 | FILE: socio_sim/web/app.py:113
ISSUE: `web_path` splits on `"socio_sim/web/"` BEFORE normalizing backslashes — a registry regenerated on Windows with `\` separators makes the split find no match, raising IndexError and 500-ing `/api/meta` (which the whole UI boots from). Latent today only because registry.json:14 happens to use forward slashes.
EVIDENCE:
```python
return "/" + rec["file_path"].split("socio_sim/web/", 1)[1].replace("\\", "/")
```
FIX: Normalize first: `rec["file_path"].replace("\\", "/").split("socio_sim/web/", 1)[1]`.
TEST: `test_asset_registry_handles_windows_separators` — feed a record with `file_path="socio_sim\\web\\static\\assets\\v4\\x.png"`, assert `web_path` returns `/static/assets/v4/x.png`.

FINDING [H2] | SEVERITY: P3 | FILE: .gitignore:1
ISSUE: Untracked local artifacts sit in the repo root un-ignored — `server.log`, `tunnel.log`, `start_ssh_tunnel.bat`, `open_vscode.bat`, `fable_phase0.txt` (per `git status`) — with no matching .gitignore entries (grep confirms none), so a careless `git add -A` commits local logs and machine-specific launchers into the research repo.
EVIDENCE:
```
?? open_vscode.bat
?? server.log
?? start_ssh_tunnel.bat
?? tunnel.log
```
FIX: Add `*.log`, `*.bat` (or the specific names) to .gitignore; relocate the tunnel/editor helpers to an ignored `local/` directory if they should persist.
TEST: `git status --porcelain | grep -E "\.log$|\.bat$"` returns nothing after `git add -A --dry-run` shows no log/bat staging.

FINDING [H3] | SEVERITY: P3 | FILE: scripts/claim_scan.py:178
ISSUE: The binary-file skip `".coverage" in {…suffixes}` is dead code — `Path(".coverage").suffix` is `""` (dotfile, not extension), so a tracked coverage database would be read as replacement-char text and scanned, producing garbage matches or silent noise.
EVIDENCE:
```python
if rel in ALLOW or path.suffix.lower() in {".png", ".gz", ".coverage"}:
    continue
```
FIX: `path.suffix.lower() in {".png", ".gz", ".coverage"}` → `path.suffix.lower() in {".png", ".gz"} or path.name == ".coverage"`.
TEST: `test_dotfile_coverage_skipped` — assert a file named `.coverage` is excluded by the filter predicate.

---
VERDICT: CONDITIONAL PASS
REASON: Default operation honors the blocked-cache and honesty invariants and no finding is remotely exploitable, but the legacy-entry downgrade path (E1) defeats the tamper protection within its own declared threat model and the CALIBRATION_REPORT.md gate exemption (C1) leaves the project's most claim-prone surface unguarded — local research use is acceptable once those two are closed.
---
TOP 5 PRIORITY FIXES (by risk):
1. [E1] - Close the legacy-entry downgrade hole in `llm_cache.resolve`: naive field-deletion converts guard-blocked text into a served cache hit; re-screen or reject dict entries lacking `record_hash` and bare-string entries.
2. [C1] - Remove `CALIBRATION_REPORT.md` from the claim-scan exemption (or add it to evidence_gate) so calibration-claim regressions on the most claim-prone surface are catchable again.
3. [E2] - Make `llm_cache.load` actually fail safe: reject non-dict JSON and catch `OSError`, instead of crashing `generate()` with AttributeError on a corrupt-but-parseable cache file.
4. [E3] - Emit a degradation event on every post-circuit-breaker template fallback in `LLMAdapter.generate`; the current silent path contradicts the "never silently" guarantee and undercounts `n_degradations` in the audit payload.
5. [E4] - Port LLMAdapter's response normalization (empty-rejection, whitespace collapse, 280-char cap) into ClaudeAdapter — the drift the shared cache module was created to prevent, currently allowing empty posts to be cached as accepted forever.
