# Fable Forensic Audit - 20260702_1602
Generated by: claude-fable-5 via subprocess
Runner: scripts/run_fable_audit.ps1
Branch: fix/p0-llm-cache-and-audit-hardening
Commit: 41a98a7

---
All findings below were verified against the working tree (branch `fix/p0-llm-cache-and-audit-hardening`), not just the pasted snapshot — line numbers are disk-exact. Surfaces the snapshot omitted (static UI, asset registry, `security.py`, `store.py`, `llm_bootstrap.py`, `evidence_gate.py`) were pulled in to cover areas E–H properly.

---

FINDING [A-01] | SEVERITY: P2 | FILE: socio_sim/config.py:92
ISSUE: Plausible-sounding behavioral rates (`eu_optout_rate=0.20`, `human_review_accuracy=0.92`, `appeal_grant_fp_rate=0.70`) are defined with no lineage label; `config.py` contains zero references to the evidence registry (verified: no `evidence`/`scenario_assumption` match in the file), so they read as empirical.
EVIDENCE:
```python
eu_optout_rate: float = 0.20        # config.py:92
human_review_accuracy: float = 0.92  # config.py:124
appeal_grant_fp_rate: float = 0.70   # config.py:126
```
FIX: `eu_optout_rate: float = 0.20` -> `eu_optout_rate: float = 0.20  # scenario_assumption (ev.scenario.eu_optout); not measured` and register `ev.scenario.*` entries for all three in the evidence registry.
TEST: `test_config_defaults_have_lineage_labels` — parse `config.py`, assert every float-literal field in the Feed/Moderation blocks has a lineage comment or registered evidence_id.

FINDING [A-02] | SEVERITY: P2 | FILE: socio_sim/web/app.py:373
ISSUE: Campaign-economics defaults (`base_ctr 0.012`, `base_cvr 0.05`, `ltv_multiplier 3.0`, `attribution_window_ticks 168`) look like industry benchmarks, carry no lineage label, and flow directly into displayed ROAS/CAC/LTV figures.
EVIDENCE:
```python
base_ctr = c_float("base_ctr", 0.012)
base_cvr = c_float("base_cvr", 0.05)
ltv_multiplier = c_float("ltv_multiplier", 3.0)
```
FIX: attach provenance in the normalized spec: `clean.append(dict(id=..., ...))` -> `clean.append(dict(id=..., ..., economics_provenance="scenario_assumption:web_defaults"))` (or comment each default `# scenario_assumption, not an industry benchmark`).
TEST: assert `_normalize_campaign_specs({"campaigns":[{"bid":1,"budget":1}]})[0]["economics_provenance"] == "scenario_assumption:web_defaults"`.

FINDING [A-03] | SEVERITY: P3 | FILE: socio_sim/web/app.py:281
ISSUE: SBM mixing matrix `[[0.02, 0.002], [0.002, 0.02]]` is a hardcoded, unlabeled numeric structure that could be mistaken for measured network density.
EVIDENCE:
```python
graph_params = {"block_sizes": [_half, _n - _half],
                "p_matrix": [[0.02, 0.002], [0.002, 0.02]]}
```
FIX: `"p_matrix": [[0.02, 0.002], [0.002, 0.02]]}` -> same value plus trailing `# synthetic_engineering: illustrative 2-block homophily, not measured` and hoist to a module constant `_SBM_P_MATRIX_SYNTHETIC`.
TEST: claim-scan-style assertion that `app.py` has no unannotated `p_matrix` literal (grep in `test_claim_scan.py`).

FINDING [A-04] | SEVERITY: P3 | FILE: socio_sim/web/app.py:265
ISSUE: Profile scale tables are duplicated in four places (app.py:265-268 `_prof_n/_prof_ticks`, app.py:724-726 `/api/meta` "profiles" — which also silently omits `aggregate_matched_prototype` — and store.py:133 `_infer_profile`); drift between copies would misreport run metadata.
EVIDENCE:
```python
_prof_n = {"quick": 1000, "test": 200, "standard": 10000,
           "aggregate_matched_prototype": 1000}.get(profile, 1000)
# app.py:724: "profiles": {"quick": ..., "test": ..., "standard": ...}  # no aggregate_matched_prototype
```
FIX: define `PROFILE_SCALES = {...}` once (e.g. in `config.py` next to the factories) -> import it in `app.py` and `store.py`; add the missing `aggregate_matched_prototype` row to `/api/meta`.
TEST: `test_profile_tables_single_source` — assert `/api/meta` profiles keys == `_PROFILES` and values match `RunConfig.<factory>()` n_agents/n_ticks.

FINDING [B-01] | SEVERITY: P3 | FILE: socio_sim/content/claude_adapter.py:22
ISSUE: Unsupported adequacy/pricing claims in code comments: "cheapest adequate model for persona posts" and (llm_adapter.py:29) "adequate for short posts" — no cited basis for "adequate" or "cheapest"; model pricing changes over time.
EVIDENCE:
```python
MODEL = "claude-haiku-4-5-20251001"  # cheapest adequate model for persona posts
# llm_adapter.py:29: "ollama": "qwen2.5:0.5b",  # ~400MB pull; adequate for short posts
```
FIX: `# cheapest adequate model for persona posts` -> `# default small model; suitability not benchmarked` (both files).
TEST: extend `STALE_PHRASES`/`RISKY_TERMS` reasoning to a unit assertion: grep both adapters for `adequate|cheapest` in `test_claim_scan.py`.

FINDING [C-01] | SEVERITY: P2 | FILE: socio_sim/web/static/app.js:864
ISSUE: A checked-and-FAILED deterministic replay is displayed as "replay n/a" in run history — `store.py:78` collapses `{checked, ok}` into one bit, so `checked=True, ok=False` (a real determinism failure) renders identically to "not checked", hiding a failed verification gate.
EVIDENCE:
```javascript
`... ${r.replay_ok ? "✓ replay" : "replay n/a"} ...`   // app.js:864
```
```python
"replay_ok": int(bool((result.get("replay") or {}).get("ok"))),  # store.py:78
```
FIX: store.py: `"replay_ok": int(bool(...ok))` -> `"replay_ok": (1 if r["ok"] else 0) if (r := result.get("replay") or {}).get("checked") else None`; app.js: `r.replay_ok ? "✓ replay" : "replay n/a"` -> `r.replay_ok === 1 ? "✓ replay" : r.replay_ok === 0 ? "✗ REPLAY FAILED" : "replay n/a"`.
TEST: `test_store_distinguishes_failed_replay_from_unchecked` — save payloads with `{"checked": True, "ok": False}` and `{"checked": False}`, assert stored values 0 vs None.

FINDING [C-02] | SEVERITY: P3 | FILE: scripts/claim_scan.py:85
ISSUE: `CALIBRATION_REPORT.md` — the one file whose name promises calibration claims — is exempt from the context-aware risky-term layer, so any future edit reintroducing "calibrated/validated" prose there is unscanned (current content is a hedged retirement note, verified; `evidence_gate.py` also never checks this file).
EVIDENCE:
```python
CONTEXT_LAYER_FILE_EXEMPT = {
    "CHANGELOG.md",
    "PLAN_P2.md",
    "CALIBRATION_REPORT.md",
}
```
FIX: remove `"CALIBRATION_REPORT.md"` from `CONTEXT_LAYER_FILE_EXEMPT` -> add the `HISTORICAL RECORD` banner to the file's first 2000 chars instead (its content is already past-tense), or add it to `evidence_gate.py`'s stale-claim report loop.
TEST: `test_calibration_report_is_scanned_or_bannered` — assert the file is not in `CONTEXT_LAYER_FILE_EXEMPT` or carries `HISTORICAL RECORD` in `text[:2000]`.

FINDING [C-03] | SEVERITY: P3 | FILE: scripts/claim_scan.py:90
ISSUE: `RISKY_TERMS` omits significance language (`significant`, `significance`) even though the audit brief's area C names it explicitly, and the payload exports a `lift_significant_bh_fdr` key (see D-01) — unhedged "statistically significant" prose in docs/UI would pass the scanner today.
EVIDENCE:
```python
RISKY_TERMS = [
    r"\bvalidat(?:ed|es|ion|ions)\b",
    r"\bcalibrat(?:ed|es|ion|ions)\b",
    ...  # no significance pattern
]
```
FIX: add `r"\bsignifican(?:t|ce|tly)\b",` to `RISKY_TERMS` (the existing negation/exemption windows will clear legitimate hedged uses).
TEST: extend `test_claim_scan.py`: a fixture line "the lift is statistically significant" in a scanned .md must produce an error.

FINDING [D-01] | SEVERITY: P3 | FILE: socio_sim/ads/measure.py:68
ISSUE: Ad measures deliberately emit both `lift_screen_positive_bh_fdr` and a legacy alias `lift_significant_bh_fdr`; the "significant" key ships in every JSON export and history payload, asserting statistical significance to downstream consumers while the UI was renamed to "screen-positive" precisely to avoid that claim.
EVIDENCE:
```python
measures[i]["lift_screen_positive_bh_fdr"] = rejected[j]
measures[i]["lift_significant_bh_fdr"] = rejected[j]
```
FIX: delete the alias line (and `measure.py:241` default) -> keep only `lift_screen_positive_bh_fdr`; update `tests/test_web.py:197` and `tests/test_ads.py:327,339,355` to the new key (grep shows those are the only consumers).
TEST: `test_ad_measures_have_no_significance_alias` — assert `"lift_significant_bh_fdr" not in measure` for every campaign in a small run.

FINDING [E-01] | SEVERITY: P1 | FILE: socio_sim/content/llm_cache.py:98
ISSUE: Tamper detection is bypassable by field deletion: `record_hash` is only verified `if "record_hash" in cached`, and an entry with `status=="accepted"` but no `record_hash` falls through to line 127 and is served verbatim. Deleting the hash (a "stale hand-edit / naive tampering" case the module docstring explicitly claims to catch) lets arbitrary swapped-in text be served without re-screening — the same failure class as the original R-CLAUDE-P0. No legit entry has `status` without `record_hash` (`make_entry` always writes both), so the state is unambiguously invalid.
EVIDENCE:
```python
if "record_hash" in cached:
    expected = record_hash(text or "", status or "", reasons)
    ...
# status == "accepted".
return CacheLookup(hit=True, text=text, degradation=None)
```
FIX: after line 96 insert: `if status is not None and "record_hash" not in cached: return CacheLookup(hit=False, text=None, degradation="cache_tampered:missing_record_hash; entry discarded and regenerated")` (covers the blocked-without-hash case too, which currently suppresses a legitimate re-screen).
TEST: `test_accepted_entry_with_stripped_record_hash_is_not_trusted` — write an accepted entry, `del cache[key]["record_hash"]`, swap `text`, reload with a counting transport; assert the swapped text is never served, transport is called once, and a `cache_tampered` degradation fires.

FINDING [E-02] | SEVERITY: P2 | FILE: socio_sim/security.py:35
ISSUE: SSRF guard is TOCTOU-vulnerable: `validate_llm_url` resolves the host via `getaddrinfo` at config-build time, but `urllib.request.urlopen` (llm_adapter.py:170) re-resolves at request time — a DNS-rebinding host passes validation then points at an internal/link-local address for the actual per-tick requests; additionally `urlopen` follows redirects, so an allowed loopback server can 302 the adapter to any unvalidated internal URL.
EVIDENCE:
```python
infos = socket.getaddrinfo(host, p.port or 80)   # security.py:35 (validation time)
...
with urllib.request.urlopen(req, timeout=self.timeout) as resp:  # llm_adapter.py:170 (request time, re-resolves + follows redirects)
```
FIX: in `_http_transport`, pin and re-check: resolve once per adapter, connect via the validated IP (or re-call `validate_llm_url(self.base_url)` per call), and pass a no-redirect opener — `urllib.request.urlopen(req, ...)` -> `opener = urllib.request.build_opener(_NoRedirect()); opener.open(req, timeout=self.timeout)` where `_NoRedirect.redirect_request` raises.
TEST: `test_transport_refuses_redirects` — local test server returning 302 to `http://169.254.169.254/`; assert the adapter degrades (no second request) rather than following.

FINDING [E-03] | SEVERITY: P3 | FILE: socio_sim/web/app.py:531
ISSUE: `llm_base_url` is SSRF-validated but `llm_model` is not: an arbitrary web-supplied string flows into `subprocess.run([ollama, "pull", model])` (llm_bootstrap.py:78), triggering an outbound pull from an attacker-chosen registry reference (e.g. `evil.example.com/model`) or a flag-like arg (`--insecure`); also `if model in have.stdout` (llm_bootstrap.py:75) is a substring match that false-positives on prefixes.
EVIDENCE:
```python
ensure_model(cfg.llm_model or "qwen2.5:0.5b", _LLM_HOST, ...)  # app.py:531
r = subprocess.run([ollama, "pull", model], env=env)           # llm_bootstrap.py:78
```
FIX: in `_build_config`: `overrides["llm_model"] = str(body["llm_model"])` -> validate with `re.fullmatch(r"[A-Za-z0-9._\-]+(:[A-Za-z0-9._\-]+)?", model)` (bare model:tag only, no registry host, no leading `-`) and reject otherwise; fix the presence check to parse `ollama list` lines exactly.
TEST: `test_build_config_rejects_registry_qualified_or_flag_llm_model` — `{"llm_model": "--insecure"}` and `{"llm_model": "evil.com/m:latest"}` raise ValueError.

FINDING [E-04] | SEVERITY: P3 | FILE: socio_sim/content/llm_adapter.py:131
ISSUE: `_fail_streak` is reset only on an *accepted* response; a successful transport call whose text is guard-blocked returns at line 128 without resetting, contradicting the documented "Reset on any success" (line 69) — interleaved failures and blocked-successes (streak never cleared) can disable a healthy server mid-run.
EVIDENCE:
```python
self.on_degradation(
    f"semantic_mismatch:{','.join(reasons)}; template text used")
return item            # blocked path: streak NOT reset
self._cache[key] = llm_cache.make_entry(text, "accepted", [])
self._save_cache()
self._fail_streak = 0  # only reached on accepted
```
FIX: move the reset to immediately after a successful transport call: after `text = " ".join(text.split())[:280]` (line 119) insert `self._fail_streak = 0` and delete line 131.
TEST: `test_blocked_response_resets_fail_streak` — two transport failures, then a guard-blocked success, then verify a subsequent call still contacts the transport (adapter not disabled).

FINDING [E-05] | SEVERITY: P3 | FILE: socio_sim/content/llm_cache.py:27
ISSUE: The trust-model docstring contradicts the code on a safety-critical rule: it says legacy entries "are trusted as accepted text", but `resolve()` (lines 88-92, 121-124) deliberately treats them as misses to force re-screening (the E1 fix). Auditors reading the documented trust model get the pre-fix, unsafe semantics.
EVIDENCE:
```python
- Legacy entries (bare strings, or dicts without a `status` field) predate
  this schema and are trusted as accepted text -- they cannot describe a
  blocked result because that status didn't exist yet when they were written.
```
FIX: replace the bullet with: "Legacy entries (bare strings, or dicts without a `status` field) were never screened under the current guard and are treated as cache misses: regenerated and freshly re-screened, never served."
TEST: doc-consistency assertion in `test_llm_adapter.py`: `assert "trusted as accepted" not in llm_cache.__doc__`.

FINDING [E-06] | SEVERITY: P3 | FILE: socio_sim/content/llm_cache.py:152
ISSUE: `save()` rewrites the cache file in place non-atomically on every new entry; a crash/kill mid-write truncates the JSON, destroying the entire replay cache (load fails safe to `{}`, but bit-identical offline replay of the completed run is silently lost and every prompt re-fires).
EVIDENCE:
```python
def save(path: Path, cache: dict) -> None:
    path.parent.mkdir(parents=True, exist_ok=True)
    path.write_text(json.dumps(cache, sort_keys=True), encoding="utf-8")
```
FIX: write-then-rename: `path.write_text(...)` -> `tmp = path.with_suffix(".json.tmp"); tmp.write_text(json.dumps(cache, sort_keys=True), encoding="utf-8"); os.replace(tmp, path)`.
TEST: `test_save_is_atomic` — monkeypatch `Path.write_text` on the final target to raise, call `save`, assert original file content is intact.

FINDING [E-07] | SEVERITY: P3 | FILE: socio_sim/content/claude_adapter.py:110
ISSUE: `ClaudeAdapter` lacks the fail-streak circuit breaker `LLMAdapter` has (llm_adapter.py:70-72): with an unreachable API or exhausted quota, every one of potentially hundreds of thousands of `generate()` calls in a standard run makes a fresh network attempt and waits out the SDK timeout — availability behavior of the two adapters has drifted despite the shared-module rationale.
EVIDENCE:
```python
except Exception as exc:  # degrade loudly, keep the run going
    self.on_degradation(f"LLM call failed: {exc!r}; template text used")
    return item
```
FIX: mirror LLMAdapter: add `self._fail_streak = 0; self._give_up_after = 3; self._disabled = False` in `__init__`, guard `generate()` with the `_disabled` early-return, increment/disable in the except block, reset on transport success.
TEST: `test_claude_adapter_disables_after_repeated_failures` — client stub raising 4 times; assert the stub is called exactly 3 times across 4 `generate()` calls and every call still reports a degradation.

FINDING [F-01] | SEVERITY: P2 | FILE: socio_sim/web/app.py:715
ISSUE: On any loopback bind, `/api/meta` hands the full access token to *any* client that can reach the port (`expose_token=True`), so the token authenticates nothing beyond "can reach loopback" — and loopback is remotely reachable via port forwarding (an SSH tunnel setup, `start_ssh_tunnel.bat`/`tunnel.log`, is present in this working tree right now): a tunneled remote client sends `Host: localhost`, passes `_host_allowed`, reads the token, and gets full POST/DELETE rights.
EVIDENCE:
```python
"token": (getattr(self.server, "access_token", None)
          if getattr(self.server, "expose_token", True) else None),
# serve(): server.expose_token = not remote   # app.py:886 — always True on loopback bind
```
FIX: honor an explicit override so tunneled deployments can lock down without a non-loopback bind: `server.expose_token = not remote` -> `server.expose_token = (not remote) and not os.environ.get("SOCIOSIM_ACCESS_TOKEN")` (setting SOCIOSIM_ACCESS_TOKEN on a loopback bind stops exposing it via /api/meta; the operator supplies it out-of-band).
TEST: `test_configured_token_is_never_exposed_via_meta` — start server with `SOCIOSIM_ACCESS_TOKEN` set on 127.0.0.1, GET `/api/meta`, assert `token is None` and `token_required is True`.

FINDING [F-02] | SEVERITY: P3 | FILE: socio_sim/web/app.py:94
ISSUE: `_host_allowed` returns True for an absent/empty Host header (`return not host or host in allowed`), so any HTTP/1.0-style client bypasses the host allow-list entirely — the DNS-rebinding read-guard is opt-out for non-browser clients on a forwarded port.
EVIDENCE:
```python
host = (headers.get("Host") or "").rsplit(":", 1)[0].strip("[]")
...
return not host or host in allowed
```
FIX: `return not host or host in allowed` -> `return host in allowed` (HTTP/1.1 requires Host; browsers always send it — the empty-host allowance protects nothing legitimate).
TEST: `test_hostless_request_rejected` — raw socket GET without a Host header returns 403.

FINDING [F-03] | SEVERITY: P3 | FILE: socio_sim/web/app.py:687
ISSUE: `_token_ok` fails open when the server object has no `access_token` attribute — any embedding of `Handler` other than `serve()` (as the test suite does) silently runs with no POST/DELETE token guard at all; a security control that defaults to off when miswired.
EVIDENCE:
```python
token = getattr(self.server, "access_token", None)
if not token:
    return True
```
FIX: fail closed with an explicit opt-out: `if not token: return True` -> `if token is None: return getattr(self.server, "allow_tokenless", False)` and set `server.allow_tokenless = True` in the test fixtures only.
TEST: `test_handler_without_token_attr_rejects_post` — bare `ThreadingHTTPServer` + Handler, POST `/api/run` returns 403 unless `allow_tokenless` is set.

FINDING [F-04] | SEVERITY: P3 | FILE: socio_sim/web/app.py:845
ISSUE: `_JOBS` grows without bound — every POST permanently retains the full multi-MB result payload in process memory (jobs are never evicted after `_STORE.save`), a slow memory-exhaustion DoS on a long-lived console session.
EVIDENCE:
```python
with _LOCK:
    _JOBS[job_id] = {"status": "running", "progress": 0.0,
                     "tick": 0, "phase": "queued"}
```
FIX: at the end of `_run_job`/`_run_compare` success path, evict old completed jobs: keep the newest N, e.g. `with _LOCK: done = [k for k,v in _JOBS.items() if v.get("status") in ("done","error")]; for k in done[:-20]: _JOBS.pop(k, None)` (results remain reopenable from `_STORE`).
TEST: `test_completed_jobs_are_evicted` — run 25 tiny jobs, assert `len(_JOBS) <= 21`.

FINDING [G-01] | SEVERITY: P3 | FILE: socio_sim/web/static/index.html:296
ISSUE: The run progress meter is a bare div (`#meterFill` width animation) with no `role="progressbar"`/`aria-valuenow`, and `#runPhase`/`#runDetail` are not in a live region — a screen-reader user gets zero feedback for the entire multi-minute run (WCAG 4.1.2 / 1.3.1); the `aria-live` regions at index.html:309-310 only update after completion.
EVIDENCE:
```html
<h3 id="runPhase">Simulating</h3>
<div class="meter"><div id="meterFill"></div></div>
<p class="mono dim" id="runDetail">tick 0</p>
```
FIX: `<div class="meter"><div id="meterFill"></div></div>` -> `<div class="meter" role="progressbar" aria-valuemin="0" aria-valuemax="100" aria-valuenow="0" aria-labelledby="runPhase"><div id="meterFill"></div></div>`; add `aria-live="polite"` to `#runDetail`; in app.js:541 set `meter.setAttribute("aria-valuenow", p)`.
TEST: Playwright/a11y assertion: during a running job, the progressbar node exposes `aria-valuenow` > 0 and `#runDetail` has `aria-live="polite"`.

FINDING [G-02] | SEVERITY: P3 | FILE: socio_sim/web/static/app.js:650
ISSUE: Node-level data in the network view (agent id, degree, ideology) is reachable only via pointer hover on a canvas — no keyboard equivalent (WCAG 2.1.1) — and ideology is encoded by color alone (blue/orange/gray, app.js:620) with no on-screen legend mapping colors to left/right (WCAG 1.4.1); the canvas `aria-label` gives only an aggregate summary.
EVIDENCE:
```javascript
cv.addEventListener("pointermove", e => { ... tip.textContent = `agent ${best.n.id} · degree ${best.n.deg} · ${g}`; ... });
const col = g => g === "L" ? "#0a84ff" : g === "R" ? "#ff9500" : "#86868b";
```
FIX: add a visible text legend to the chart subtitle: `"depth = front/back · size = degree · colour = ideology"` -> `"… · colour = ideology (blue left · orange right · grey centre)"`, and render a keyboard-accessible top-hubs table (`<details><table>` of id/degree/ideology) below the canvas as the non-pointer equivalent.
TEST: DOM assertion after `renderNetwork`: legend text includes "blue left" and a `table` sibling with one row per rendered hub exists.

FINDING [H-01] | SEVERITY: P2 | FILE: scripts/evidence_gate.py:14
ISSUE: The evidence gate defines a `sha()` helper but never calls it — the 92 per-asset `sha256` values recorded in registry.json are never verified against the files on disk, so a swapped/corrupted bundled PNG passes the gate as long as the asset *count* stays ≥ 92; the gate's file-integrity intent is dead code.
EVIDENCE:
```python
def sha(path: Path) -> str:
    return hashlib.sha256(path.read_bytes()).hexdigest()
# main(): only checks len(data.get("assets", [])) < 92 — sha() has no call sites
```
FIX: in `main()` after the count check, add: `for rec in data.get("assets", []): p = ROOT / rec["file_path"]; if not p.is_file() or sha(p) != rec["sha256"]: errors.append(f"asset integrity: {rec['file_path']}")`.
TEST: `test_evidence_gate_fails_on_modified_asset` — copy repo asset to tmp registry fixture, flip one byte, assert gate returns 1 with an "asset integrity" error.

FINDING [H-02] | SEVERITY: P3 | FILE: pyproject.toml:49
ISSUE: `contact-sheet-v4.png` exists in the v4 asset directory (93 PNGs on disk vs 92 registry entries, verified) and ships in the wheel via the `static/assets/v4/*.png` package-data glob, but is absent from registry.json — a distributed image that bypasses the asset registry/evidence pipeline entirely.
EVIDENCE:
```toml
"static/assets/v4/*.png",
```
```text
disk: 48 feed-cover + 32 ad-creative + 12 editorial + contact-sheet-v4.png = 93; registry "file_path" count = 92
```
FIX: either register it (add a `role: "qa_contact_sheet"` record with sha256 + human_review status) or exclude it from distribution: move it out of `static/assets/v4/` into a non-packaged `qa/` directory.
TEST: extend `test_bundled_v4_assets_exist_and_are_unignored`: assert every `*.png` under `assets/v4/` has a matching registry record (set equality, not counts).

FINDING [H-03] | SEVERITY: P3 | FILE: socio_sim/web/app.py:113
ISSUE: `_asset_registry.web_path` does `split("socio_sim/web/", 1)[1]` — any registry record whose `file_path` lacks that exact marker (or uses backslashes before the `.replace` runs, since split happens first) raises IndexError, turning every `/api/meta` request into a 500 and bricking the dashboard on a malformed registry row.
EVIDENCE:
```python
def web_path(rec):
    return "/" + rec["file_path"].split("socio_sim/web/", 1)[1].replace("\\", "/")
```
FIX: normalize before splitting and skip bad rows: `return "/" + rec["file_path"].split(...)[1]...` -> `norm = rec.get("file_path", "").replace("\\", "/"); return "/" + norm.split("socio_sim/web/", 1)[1] if "socio_sim/web/" in norm else None` and filter `None` from the three list comprehensions.
TEST: `test_asset_registry_tolerates_malformed_file_path` — monkeypatch registry with one bogus `file_path`, assert `_asset_registry()` returns without raising and omits the bad row.

---
VERDICT: CONDITIONAL PASS
REASON: No P0-class safety regression remains (blocked cache entries are never served, XSS sinks are escaped, SQL is parameterized, traversal is contained), but the P1 record-hash-stripping bypass defeats the cache trust model's own stated guarantee and the P2s (token exposure over forwarded loopback — a live risk given the SSH tunnel scripts in this working tree — and failed-replay masking) must land before this branch is treated as remediated.
---
TOP 5 PRIORITY FIXES (by risk):
1. [E-01] - Reject cache entries that carry a `status` but no `record_hash`; deleting the hash currently bypasses tamper detection and serves unscreened text verbatim.
2. [F-01] - Stop exposing the access token via `/api/meta` when `SOCIOSIM_ACCESS_TOKEN` is configured; with the SSH tunnel in use, any tunneled client currently self-serves full POST/DELETE rights.
3. [C-01] - Distinguish "replay checked and FAILED" from "replay not checked" in the store and history UI; a determinism failure is currently rendered as benign "replay n/a".
4. [E-02] - Pin DNS/disable redirects in the LLM transport; the SSRF guard validates at config time only and `urlopen` re-resolves and follows redirects at request time.
5. [H-01] - Wire the dead `sha()` helper into `evidence_gate.py` so the registry's recorded per-asset sha256s are actually verified against disk.
